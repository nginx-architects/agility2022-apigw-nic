apiVersion: k8s.nginx.org/v1
kind: VirtualServerRoute
metadata:
  name: locations
  namespace: apiv2
spec:
  host: api.example.com
  upstreams:
  - name: locations
    service: locations
    port: 80
  subroutes:
  - path: /api/locations/v2
    action:
      pass: locations
